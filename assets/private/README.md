# Private assets

Place your purchased Linux build here:

```text
dwarf_fortress_53_16_linux.tar.bz2
```

The file is intentionally excluded by `.gitignore` and must never be committed.

The DFCapture source and DFHack are fetched from GitHub at pinned versions during the Docker build.
